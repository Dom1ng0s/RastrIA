PORT=3000

# Consumido em build-time pelo Vite (frontend/) — precisa estar setada como
# variável de ambiente do serviço no Railway antes do build, não só aqui.
VITE_API_URL=http://localhost:8000/api
