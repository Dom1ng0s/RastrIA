{
  "name": "rastria",
  "version": "0.1.0",
  "private": true,
  "description": "rastrIA",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "serve -s frontend/dist -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.3"
  }
}
